
@ 해야할 것

1) token type 찾기
    이거는 start, end 찾으면서 같이 해도 될 것 같다.
    처음부터 읽어올 때 처음 들어오는 값이 무엇이냐에 따라 정해줘야할 것 같다.
    처음에 "로 시작하면 스트링
    숫자로 시작하면 int 
    [로 시작하면 array
    {로 시작하면 object
    : 이거랑 , 이거 역할을 잘 사용해야 할 ㅏ것 같은데 너무 잠와서 일단 오늘은 여기까지
    
2) 파싱 함수의 역할
    parser() 함수 내에서 token structure를 생성해도 될 것 같다.
    문서의 끝까지 모두 파싱하면 만들어진 토큰들을 array에 넣어서 return 하면 좋을 것 같다.
    